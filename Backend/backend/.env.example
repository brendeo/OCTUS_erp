SERVER_PORT=8080

# --- Frontend local (sem login) ---
SPRING_PROFILES_ACTIVE=mock
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# --- Desenvolvimento com PostgreSQL + JWT + dados demo ---
# SPRING_PROFILES_ACTIVE=dev
# DATABASE_URL=jdbc:postgresql://localhost:5432/gestorpyme
# DATABASE_USER=gestor
# DATABASE_PASSWORD=gestor123
# JWT_SECRET=troque-esta-chave-por-uma-segura-com-pelo-menos-32-caracteres
# JWT_EXPIRATION_MS=86400000

# --- Apresentação / demo disciplina (sem seed demo, Swagger desligado) ---
# SPRING_PROFILES_ACTIVE=prod
# DATABASE_URL=jdbc:postgresql://localhost:5432/gestorpyme
# DATABASE_USER=gestor
# DATABASE_PASSWORD=gestor123
# JWT_SECRET=obrigatorio-chave-unica-minimo-32-caracteres-para-prod
# JWT_EXPIRATION_MS=86400000
# CORS_ALLOWED_ORIGINS=https://seu-frontend.example.com
